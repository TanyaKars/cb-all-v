{
  "name": "cb",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "cy:open:v1": "cypress open --browser chrome --env configFile='v1' --config-file cypress.v1.config.ts",
    "cy:open:v2": "cypress open --browser chrome --env configFile='v2' --config-file cypress.v2.config.ts",
    "cy:open:v3": "cypress open --browser chrome --env configFile='v2' --config-file cypress.v3.config.ts",
    "cy:open:v4": "cypress open --browser chrome --env configFile='v2' --config-file cypress.v4.config.ts",
    "cy:open:v5": "cypress open --browser chrome --env configFile='v2' --config-file cypress.v5.config.ts",
    "cy:run:v1": "cypress run --headless --browser chrome --env configFile='v1' --config-file cypress.v1.config.ts",
    "cy:run:v2": "cypress run --headless --browser chrome --env configFile='v2' --config-file cypress.v2.config.ts",
    "cy:run:v3": "cypress run --headless --browser chrome --env configFile='v3' --config-file cypress.v3.config.ts",
    "cy:run:v4": "cypress run --headless --browser chrome --env configFile='v4' --config-file cypress.v4.config.ts",
    "cy:run:v5": "cypress run --headless --browser chrome --env configFile='v5' --config-file cypress.v5.config.ts"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/TanyaKars/cb-all-v.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/TanyaKars/cb-all-v/issues"
  },
  "homepage": "https://github.com/TanyaKars/cb-all-v#readme",
  "devDependencies": {
    "@faker-js/faker": "^7.6.0",
    "axe-core": "^4.5.0",
    "cypress": "^10.11.0",
    "cypress-axe": "^1.0.0",
    "cypress-dotenv": "^2.0.0",
    "dotenv": "^16.0.3",
    "typescript": "^4.8.4"
  }
}
